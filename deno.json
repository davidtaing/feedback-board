{
  "tasks": {
    "dev": "deno run --watch main.ts",
    "test": "deno test",
    "check": "deno check . && deno lint && deno fmt --check"
  },
  "fmt": {
    "lineWidth": 100
  },
  "lint": {
    "rules": {
      "tags": ["recommended"]
    }
  }
}
